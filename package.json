{
  "name": "next-inspect",
  "version": "0.1.0",
  "description": "TypeScript npm package boilerplate",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "next-inspect": "dist/cli/bin.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "dev": "tsc -w -p tsconfig.build.json",
    "clean": "node -e \"require('fs').rmSync('dist',{recursive:true,force:true})\"",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "intercept": "npm run build && node dist/cli/bin.js intercept",
    "interceptor": "npm run intercept",
    "prepublishOnly": "npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "axios": "^1.8.4",
    "ws": "^8.18.1"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "@types/ws": "^8.18.0",
    "typescript": "^5.8.2"
  }
}
